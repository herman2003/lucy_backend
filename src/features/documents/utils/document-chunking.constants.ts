/** Target chunk size in characters (~800 tokens). */
export const CHUNK_TARGET_CHARS = 3200;

/** Overlap between consecutive chunks in characters (~100 tokens). */
export const CHUNK_OVERLAP_CHARS = 400;

/** Approximate characters per token for `tokenEstimate` (chars / 4). */
export const CHARS_PER_TOKEN_ESTIMATE = 4;
